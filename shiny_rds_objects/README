This folder contains all of the RDS objects for the shiny analysis. To use the RDS objects, download an RDS file and in Rstudio
use the following (This requires sleuth to be installed on your machine. 
If you do not have sleuth installed, visit this page: http://pachterlab.github.io/sleuth/download)

library(sleuth)
so <- readRDS("YOUR_RDS_FILE.rds")
shiny_live(so)
